# Node.js
node_modules/
# Root-only: the tsc build output (npm package). The committed, self-contained
# plugin bundle at plugins/mcp-gemini-server/dist/ is intentionally NOT ignored.
/dist/
*.log
coverage/
.coverage
*.tsbuildinfo

# IDEs
.idea/
.vscode/
*.swp
*.swo

# Environment variables
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Local-only / non-published (internal docs, tooling, deploy)
/tmp/
/refs/
/configs/
/specs/
/report/
/scripts/
/ansible/
/Makefile
/CLAUDE.md
/.claude/
/.claude.json
/.specify/

# secretlint local-only denylist (company/personal terms — never committed)
.secretlintrc.local.json

# Rust port build artifacts (028-rust-port branch)
/target/
/tmp/
